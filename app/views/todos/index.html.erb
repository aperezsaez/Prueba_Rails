<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
  <h1>Index</h1>
      <% @todos.each do |i| %>
       <div class = "well"> <h2><%= link_to i.description, todo_path(i.id) %></h2>
       <p><%= link_to "Check", fill_path(i.id)%></p>
       <p><%= link_to "Show", todo_path(i.id)%></p>
       <p><%= link_to "Edit", cambiar_path(i.id)%></p>
       <p><%= link_to "Borrar", i, method: :delete %></p>
       </div>
       <% end %>
     <p class = "btn btn-primary"><%= link_to "Nuevo ToDo", todos_new_path %></p>

  </body>
</html>
